const { theme: themeCollection } = uniweb.getServices();

const { default: defaultExport, apply, aspectRatio, css, lineClamp, properties, screen, styled, styledComponents, styles, theme, tw, twClass } = themeCollection;

export default defaultExport;

export { apply, aspectRatio, css, lineClamp, properties, screen, styled, styledComponents, styles, theme, tw, twClass };
